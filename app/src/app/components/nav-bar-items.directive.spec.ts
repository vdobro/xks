import { NavBarItemsDirective } from './nav-bar-items.directive';

describe('NavBarItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new NavBarItemsDirective();
    expect(directive).toBeTruthy();
  });
});
